# Example env file for local development
# Copy to .env or use explicitly with --env-file
DB_HOST=postgres
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=locally_db
DATABASE_URL=postgres://user:password@postgres:5432/locally_db
